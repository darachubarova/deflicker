version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./results:/app/results
      - ./examples:/app/examples
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
