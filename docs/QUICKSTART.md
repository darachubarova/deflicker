# 🎭 Mask Stabilization - Быстрый старт

## Что это?

Система для уменьшения "мерцания" при покадровой сегментации видео с использованием нейросети DeepLabv3.

---

## Быстрая установка (5 минут)

### Шаг 1: Windows-сервер

Скачайте и запустите `setup_windows.ps1` в PowerShell от администратора:

```powershell
# Или выполните вручную:
Start-Service sshd
Set-Service -Name sshd -StartupType Automatic
```

### Шаг 2: Docker-контейнер (Jupyter)

```bash
# Скопируйте скрипт
cp start_system.sh /tf/darachubarova/defliker/
chmod +x /tf/darachubarova/defliker/start_system.sh
cd /tf/darachubarova/defliker

# Настройте SSH-ключи (один раз)
./start_system.sh --setup-keys

# Запустите систему
./start_system.sh
```

### Шаг 3: Готово!

Откройте в браузере: **http://ВАШ_IP:8080**

---

## Команды управления

```bash
./start_system.sh              # Запуск
./start_system.sh --stop       # Остановка
./start_system.sh --status     # Статус
./start_system.sh --logs       # Логи
./start_system.sh --tunnel     # Только туннель (интерактивно)
./start_system.sh --setup-keys # Настройка SSH-ключей
```

---

## Использование веб-интерфейса

1. **Загрузите видео** — перетащите файл в зону загрузки
2. **Выберите класс** — Person, Car, Dog и т.д.
3. **Выберите метод** — Moving Average, Median Filter, Exponential Smoothing
4. **Нажмите Stabilize** — дождитесь обработки
5. **Смотрите результаты** — метрики IoU и покадровое сравнение

---

## Методы стабилизации

| Метод | Параметр | Когда использовать |
|-------|----------|-------------------|
| Moving Average | Window Size (3-9) | Общее сглаживание |
| Median Filter | Window Size (3-9) | Удаление выбросов |
| Exponential Smoothing | Alpha (0.1-0.9) | Адаптивное сглаживание |

---

## Архитектура

```
Браузер → Windows (туннель) → Docker-контейнер → DeepLabv3 → Результат
```

Вся обработка происходит на сервере с GPU. Windows служит только мостом.

---

## Устранение проблем

**Туннель отключается:**
```bash
./start_system.sh --tunnel  # Интерактивный режим
```

**Порт занят:**
```bash
./start_system.sh --stop
./start_system.sh
```

**Нет подключения к Windows:**
- Проверьте: `Get-Service sshd` (должен быть Running)
- Проверьте файрвол: порты 22, 8000, 8080

---

## Файлы проекта

```
/tf/darachubarova/defliker/
├── start_system.sh      # Скрипт запуска
├── src/                 # Backend (FastAPI)
├── frontend/            # Веб-интерфейс
├── logs/                # Логи
└── results/             # Результаты обработки
```

---

*Полная документация: METHODOLOGY.md*
